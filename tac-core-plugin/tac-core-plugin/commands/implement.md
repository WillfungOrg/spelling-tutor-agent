# Implement the following plan

**TAC Reminder:** Before implementing:
1. **Knowledge Gaps:** Verify you have all necessary context (use `/knowledge_gap_check` if unsure)
2. **Validate Spec:** Ensure the plan is clear and AI-executable
3. **Closed-Loop:** Plan includes validation commands you'll run after implementation

See `.claude/TAC-PRINCIPLES.md` for framework details.

---

Follow the `Instructions` to implement the `Plan` then `Report` the completed work.

## Instructions
- Read the plan, think hard about the plan and implement the plan.
- Follow TAC Principle 4 (Closed-Loop Validation): Run validation commands from the plan as you complete each phase.

## Plan
$ARGUMENTS

## Report
- Summarize the work you've just done in a concise bullet point list.
- Report the files and total lines changed with `git diff --stat`